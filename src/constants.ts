export enum ChainId {
  MAINNET = 1,
  ROPSTEN = 3,
  RINKEBY = 4,
  GÖRLI = 5,
  KOVAN = 42
}

export enum SolidityType {
  uint8 = 'uint8',
  uint256 = 'uint256',
  bytes4 = 'bytes4',
  bytes32 = 'bytes32',
  bytes = 'bytes',
  address = 'address',
  string = 'string'
}
